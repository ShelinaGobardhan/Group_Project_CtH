var find = function() {
    var MOVIEFIND = prompt("Are you a male or female?").toUpperCase();
        if (MOVIEFIND === 'MALE' || MOVIEFIND === 'FEMALE') {
            prompt("Will you watch this movie alone?");
        }
};

var random = function() {
    
}
